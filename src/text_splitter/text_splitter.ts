/*
    Returns text split into sentences
    Splits the text based on punctuations such as periods, question marks, and exclamation marks
*/
const splitSentences = (text: string): Array<string> =>
{
    return null;
}



const splitParagraphs = (paragraphs: Array<string>): Array<string> =>
{
    return null;
}

export default [splitParagraphs, splitParagraphs];